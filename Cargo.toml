[package]
name = "zed-fish-lsp"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Fish shell language support with fish-lsp integration for Zed"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"
